[tox]
envlist = py34,py27
skipsdist = True

[testenv]
deps =
    -r{toxinidir}/requirements_test.txt
setenv =
    PYTHONPATH = {toxinidir}:{toxinidir}
commands =
    coverage run -p --source=docker -m py.test -v tests
